import { Heading, VStack } from '@chakra-ui/react'

export const App = () => {
  return (
    <VStack backgroundColor={'primary'} minHeight={'100vh'} minWidth='100vw' justifyContent={'center'} alignItems={'center'}>
      <Heading>Hola</Heading>
    </VStack>
  )
}
